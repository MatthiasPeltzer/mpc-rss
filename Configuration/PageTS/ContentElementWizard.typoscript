mod.wizards.newContentElement.wizardItems.plugins {
  elements {
    mpcrss_feed {
      iconIdentifier = mpc-rss-plugin
      title = MPC RSS Feed
      description = Display RSS feeds grouped by category
      tt_content_defValues {
        CType = mpcrss_feed
      }
    }
  }
  show := addToList(mpcrss_feed)
}
